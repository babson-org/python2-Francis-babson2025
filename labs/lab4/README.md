
# OOP Portfolio Lab — Multi-File, Test-Driven

## TODOs for students
See files under `portfolio/` and the TODO in `portfolios.save()`.

## Run tests
pip install -r requirements.txt
pytest -q

## Run app
python main.py
