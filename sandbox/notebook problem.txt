# 1. Reset any ongoing merge
git merge --abort || true

# 2. Throw away the student's copy of the bad notebook
git fetch upstream
git checkout upstream/main -- classes/week02/monday/exercise/s1-12.ipynb

# 3. Stage and commit the teacher's version
git add classes/week02/monday/exercise/s1-12.ipynb
git commit -m "Reset s1-12.ipynb to teacher's version"

# 4. Try the sync again
git upstream-save
